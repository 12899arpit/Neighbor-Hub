[phases.setup]
nixPkgs = ["elixir", "erlang"]

[phases.build]
cmds = [
  "mix local.hex --force",
  "mix local.rebar --force",
  "mix deps.get --only prod",
  "mix compile",
  "mix assets.deploy"
]

[phases.release]
cmds = ["mix ecto.migrate"]

[start]
cmd = "mix phx.server"